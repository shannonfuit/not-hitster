<div class="page <%= page.side %>">
  <div class="cards-grid">
    <% page.cards.each do |song| %>
      <% if page.side == :front %>
        <%= render 'songs/card_front', song: song %>
      <% else %>
        <%= render 'songs/card_back', song: song %>
      <% end %>
    <% end %>
  </div>
</div>